1. 队员拉取代码失败：检查是否复制了正确的仓库地址，或是否有GitHub账号（需注册账号并告知发起人，添加为协作者）；

2. 数据库打开报错：确认文件路径无中文（如避免 D:\中文文件夹\bill_db.sqlite ），或重新从GitHub拉取最新的数据库文件；

3. 代码运行报错“缺少模块”：让队员重新执行 pip install 缺少的模块名 （如 pip install pandas ）。